<%# Body content for a modal overlay's loading placeholder. Wrapped
    in the modal chrome at template-emission time by `overlay_stack_tag`
    (which renders `_modal.html.erb` with `loading: true` to suppress
    the Stimulus controller, close button, and overlay title/footer slots). %>
<div class="turbo-overlay-loading__body">
  <span class="turbo-overlay-loading__spinner" aria-hidden="true"></span>
</div>
