<%# Body content for a hover-hint's pending placeholder. Wrapped in
    the hint chrome at template-emission time by `overlay_stack_tag`. %>
<span class="turbo-overlay-loading__spinner" aria-hidden="true"></span>
